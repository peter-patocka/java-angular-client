language: node_js
node_js:
  - "10.16.0"
dist: trusty
sudo: required

branches:
  only:
  - master

before_script:
  - npm install -g @angular/cli
  
script:
  - npm run lint
  - npm run test-headless
  # - npm run e2e
  - ng build --prod --base-href https://peter-patocka.github.io/java-angular-client/

deploy:
  provider: pages
  skip_cleanup: true
  github_token: d00780f9b03f9c5fdb1eff54a8cf0f8ece70f325
  local_dir: dist/java-angular-client
  on:
    branch: master